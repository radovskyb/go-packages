package main

func main() {
	name := "Benjamin"
	// The same as print, except println()
	// contains a newline (\n) character at
	// the end of the line as well as concatenation
	// using a comma, places a space before the
	// concatenated item, whilst in print()
	// it doesn't add the space.
	println("Hello", name)
}
